# Vehicle Query Site

Cloudflare Workers 车型查询 POC 的代码目录。

计划采用 React、Vite、TypeScript、Cloudflare Workers 和 D1。具体开发任务、API 列表与
验收标准见项目根目录的 [`TASKS.md`](../TASKS.md)。

站点尚未初始化；下一步从 `TASKS.md` 的 M1 开始搭建工程。
